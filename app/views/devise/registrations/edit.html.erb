<div class="text-center m-4">
  <h2>Editer vos informations</h2>
</div>
<div class="border-form">
  <div class=" col col-12 col-sm-12 d-flex flex-column">
    <div class="card mb-3 ">
      <div class="card-header text-center ">
        <h3>Informations de connections</h3>
        <small> Les champs avec un * sont obligatoires</small>
      </div>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }, data: { turbo: false }) do |f| %>
        <% resource.errors.full_messages.each do |msg| %>
          <div class="alert alert-danger"><%= msg %></div>
        <% end %>
        <div class="d-flex flex-column">
          <div class="d-flex flex-column align-items-center text-center">
            <%= f.label :email, class: "m-2"%>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "m-0" %>
          </div>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>
          <hr>
          <div class="d-flex flex-column align-items-center text-center">
            <%= f.label :password, class: "m-2" %>
            <%= f.password_field :password, autocomplete: "new-password" %>
            <% if @minimum_password_length %>
              <em><%= @minimum_password_length %> caractères minimum</em>
            <% end %>
          </div>
          <hr>
          <div class="d-flex flex-column align-items-center text-center">
            <%= f.label :password_confirmation, class: "m-2" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
          </div>
          <hr>
          <div class="d-flex flex-column align-items-center text-center pb-3">
            <%= f.label :"Mot de passe actuel*", class: "m-2" %>
            <%= f.password_field :current_password, autocomplete: "current-password" %>
          </div>
        </div>
      </div>
      <div class="card">
        <div class="card-header text-center">
          <h3>Informations personnels</h3>
        </div>
        <div class="d-flex flex-column align-items-center">
          <div class="d-flex flex-column align-items-center my-2">
            <%= f.label :first_name, class: "m-2" %>
            <%= f.text_field :first_name, autocomplete: "first_name" %>
          </div>
          <div class="d-flex flex-column align-items-center my-2">
            <%= f.label :last_name, class: "m-2" %>
            <%= f.text_field :last_name, autocomplete: "last_name" %>
          </div>
          <div class="d-flex flex-column align-items-center my-3">
            <%= f.label :birth_date, class: "m-2" %>
            <%= f.date_field :birth_date, autocomplete: "birth_date" %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="text-center mt-2">
    <small>Merci de rentrer votre mot de passe actuel pour valider les modifications</small>
  </div>
  <div class="actions d-flex my-5 justify-content-center">
    <%= f.submit "Update", class: "btn btn-success" %>
  </div>
<% end %>
<hr>
<div class="m-3 d-flex flex-column align-items-center">
  <h3 class="m-3">Supprimer votre compte</h3>
  <%= button_to "Supprimer votre compte", registration_path(resource_name), class: "btn btn-danger", data: { confirm: "Are you sure?" }, method: :delete %>
</div>
</div>
