<%= form_with(model: subcription) do |form| %>
  <% if subcription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subcription.errors.count, "error") %> prohibited this subcription from being saved:</h2>

      <ul>
        <% subcription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
